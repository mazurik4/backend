# База данных — содержит реальные заявки клиентов (имена, телефоны).
# Не должна попадать в публичный репозиторий.
requests.db
requests.db-wal
requests.db-shm

# Файл с переменными окружения (секретные ключи) — если будете использовать локально
.env

# Служебные файлы Python
__pycache__/
*.pyc
venv/
.venv/

# Служебные файлы IDE/ОС
.vscode/
.DS_Store
